# Copy to .env.local for local overrides.
# In dev this is unnecessary — Vite proxies /api to localhost:8000 (see vite.config.js).
# In production (Azure Static Web Apps), set this to your deployed backend URL, e.g.
# VITE_API_BASE_URL=https://p51-sentiment-api.azurewebsites.net
VITE_API_BASE_URL=
